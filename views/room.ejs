<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Video Coll - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-purple-600 via-indigo-600 to-blue-600 text-white min-h-screen flex flex-col items-center justify-center">

  <h1 class="text-5xl font-bold mb-10 tracking-wide">🎥 VideoColl</h1>

  <div class="bg-white text-black p-8 rounded-2xl shadow-lg w-full max-w-md space-y-6">
    
    <!-- Create Room -->
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Create a Room</h2>
      <form action="/create-room" method="POST">
        <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 px-4 rounded-xl font-semibold">
          ➕ Create New Room
        </button>
      </form>
    </div>

    <hr class="border-gray-300"/>

    <!-- Join Room -->
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Join a Room</h2>
      <form action="/join-room" method="GET" class="space-y-4">
        <input type="text" name="roomId" placeholder="Enter Room ID" required 
               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500"/>
        <button type="submit" class="w-full bg-green-600 hover:bg-green-700 text-white py-2 px-4 rounded-xl font-semibold">
          ✅ Start Meeting
        </button>
      </form>
    </div>
  </div>

  <footer class="mt-10 text-white/80 text-sm">
    &copy; <%= new Date().getFullYear() %> VideoColl App. All rights reserved.
  </footer>
</body>
</html>
